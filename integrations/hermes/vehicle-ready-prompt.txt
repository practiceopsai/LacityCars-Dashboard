AUTOMATED VEHICLE STOCKING JOB FROM THE AUTHENTICATED DASHBOARD.

This HMAC-authenticated event authorizes only the supplied vehicle or store-specific batch. Load and follow the vehicle-stock-in skill and the repository's .hermes.md contract. Never broaden the store, vehicle, or batch scope.

Request ID: {request_id}
Callback URL: {callback_url}
Event type: {event_type}
Store: {store.name} ({store.code})
AutoSoft instance: {store.autosoft_instance}
Stock prefix: {store.stock_prefix}
Authorized start (UTC): {schedule.starts_at}
Authorized start (Eastern): {schedule.eastern}
Authorized start (Pacific): {schedule.pacific}

Single-vehicle fields (vehicle.ready):
VIN: {vehicle.vin}
Vehicle: {vehicle.model}
Proposed stock number: {vehicle.stock_number}
Freight amount: {freight.amount}
Freight evidence: {freight.evidence}
Corrections: {corrections}

Batch fields (vehicle.batch_ready):
Batch: {batch}
Ordered vehicles: {vehicles}

The authorized start is a hard not-before boundary because AutoSoft is a shared account. Compare current time with schedule.starts_at. If current time is earlier, do not open or modify the stock sheet, NextGear, AutoSoft, or any live stocking system; fail closed and report the premature trigger. The schedule never authorizes concurrent AutoSoft sessions.

For vehicle.ready, run the existing one-vehicle procedure and callback lifecycle. Acquire the same foreground RDP lease described below before AutoSoft; keep it across consecutive verified actions, and recheck after a focus/window change or unexplained screen.

For vehicle.batch_ready, perform one batch RAG lifecycle and one sequential desktop session:
1. Parse the complete Ordered vehicles JSON before live work. Its array length MUST equal batch.vehicle_count and every object must have a complete request_id and 17-character VIN. If integrity fails, send one FAILED callback for any complete child with `failure_scope=BATCH`; the dashboard will fail the claimed siblings safely. Never work from a truncated manifest.
2. Validate the named store, AutoSoft instance, schedule, duplicate VINs, compact freight evidence, source, accounting inputs, and every VIN before live entry. The freight evidence is intentionally summarized as loadId, loadPrice, distinctVinCount, matchedRowNumbers, and fetchedAt; use the supplied amount and re-check live evidence when required. Never estimate missing freight.
3. Deduplicate every supplied VIN against the dashboard ledger, the named store's stocking sheet, and the run checkpoints before writing. A verified existing sheet row is reused and never appended again. A terminal AutoSoft/readback checkpoint is never reposted. Update only missing stocking-sheet rows in one pass on the named store's sheet, then perform a second row-by-row verification pass.
4. Before AutoSoft and at the start of every vehicle, run `python tools/focus_autosoft_rdp.py --expected-title <payload store.autosoft_instance>` (quote the actual value in the shell when it contains spaces), capture the exact main RDP window, and visibly verify the intended control. Treat this as a foreground lease across consecutive actions on the verified screen; re-run it only after a focus/window change or unexplained state. Never send background input to mstsc.exe and never type after an `unverifiable` action until a fresh capture proves state.
5. Finish preflight by building one local ordered posting manifest containing every verified vehicle-specific field. Do not revisit the sheet, dispatch workbook, or source browser between AutoSoft records unless a verification fails. Keep one foreground LA/Columbia session, reuse the already-open Accounting/Inventory workflow, and move directly to the next fresh record after each verified save.
6. Process vehicles strictly in payload order. Send PROCESSING for a child immediately before touching its AutoSoft record. For each text field use verified clipboard entry: focus the intended control, Ctrl+A, copy/paste the exact manifest value, then visually/readback verify it. Start a fresh record, clear and visibly verify the entire VIN field is blank, paste only that VIN, decode before Line, apply vehicle-specific values and the verified store template, verify accounting, save, read back, and checkpoint before continuing. Never paste a multi-vehicle block into AutoSoft.
7. Known LA startup recovery: `CRITICAL ERROR ID 227, Position Error #7-0 in AAJ3` shown on the LA City home screen is a one-dismiss startup alert, not automatically a store-wide outage. Click OK once, require the normal `LA City Cars` home screen, open Accounting, and continue only if the password/PIN prompt appears and accepts the configured saved credential. If the alert reappears inside the posting workflow, Accounting cannot open, authentication is rejected, or controls remain unusable, then fail the batch as `AUTOSOFT_UNAVAILABLE`. Never dismiss any other critical/runtime error by analogy.
8. A vehicle-specific failure gets its own FAILED callback and the batch continues. A wrong store, lost/unusable AutoSoft session, rejected accounting authentication, persistent AAJ3/runtime error, or other store-wide blocker requires ONE current-child FAILED callback with `failure_scope=BATCH`; do not spend repeated turns trying blind recovery. The dashboard deterministically fails every still-claimed sibling and releases the shared lock.
9. Update the RAG record after every vehicle checkpoint and finish with a concise batch summary. Never repost a vehicle whose callback/checkpoint is already terminal.

The callback's stock number, ACV, freight, final total, source/run summary, and RAG commit must come from verified evidence. A missing source, missing defensible freight, duplicate/store conflict, rejected PIN, unexpected screen, or accounting mismatch must fail closed; never estimate or bypass a gate.
