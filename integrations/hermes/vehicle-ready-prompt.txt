AUTOMATED VEHICLE STOCKING JOB FROM THE AUTHENTICATED DASHBOARD.

This HMAC-authenticated event authorizes only the supplied vehicle or store-specific batch. Load and follow the vehicle-stock-in skill and the repository's .hermes.md contract. Never broaden the store, vehicle, or batch scope.

Request ID: {request_id}
Callback URL: {callback_url}
Event type: {event_type}
Store: {store.name} ({store.code})
AutoSoft instance: {store.autosoft_instance}
Native RDP window title: {store.rdp_window_title}
Stock prefix: {store.stock_prefix}
Authorized start (UTC): {schedule.starts_at}
Authorized start (Eastern): {schedule.eastern}
Authorized start (Pacific): {schedule.pacific}

Single-vehicle fields (vehicle.ready):
VIN: {vehicle.vin}
Vehicle: {vehicle.model}
Proposed stock number: {vehicle.stock_number}
Freight amount: {freight.amount}
Freight evidence: {freight.evidence}
Corrections: {corrections}

Batch fields (vehicle.batch_ready):
Batch: {batch}
Ordered vehicles: {vehicles}

The authorized start is a hard not-before boundary because AutoSoft is a shared account. The dashboard worker already delays delivery until this boundary, but verify it once using a fresh UTC clock value produced in this webhook session immediately after writing `vehicle-stock-in/active-request.json`. Compare only that fresh value with schedule.starts_at. Never reuse or infer the current time from a prior run log, result JSON, checkpoint, RAG search excerpt, model context, file timestamp, or another webhook session. If an older artifact disagrees with the fresh clock, the fresh clock is authoritative. If and only if that fresh value is earlier, do not open or modify the stock sheet, NextGear, AutoSoft, or any live stocking system; fail closed and report both exact timestamps. The schedule never authorizes concurrent AutoSoft sessions.

For vehicle.ready, run the existing one-vehicle procedure and callback lifecycle. Acquire the same foreground RDP lease described below before AutoSoft; keep it across consecutive verified actions, and recheck after a focus/window change or unexplained screen.

For vehicle.batch_ready, perform one batch RAG lifecycle and one sequential desktop session:
1. Before reading any prior artifact, overwrite exactly `vehicle-stock-in/active-request.json` with the exact current webhook payload. Never write a request beneath `.hermes/` because Hermes protects that instruction namespace and a headless approval would time out. Never discover or reuse another request JSON from an earlier retry. Parse the complete Ordered vehicles JSON before live work. Its top-level request_id must equal the current webhook request ID, every child request_id must begin with that current ID plus `:`, its array length MUST equal batch.vehicle_count, and every object must have a 17-character VIN. If integrity fails, send one FAILED callback for a current complete child with `failure_scope=BATCH`; the dashboard will fail the claimed siblings safely. Never work from a stale or truncated manifest.
2. Validate the named store, AutoSoft instance, native RDP title, schedule, duplicate VINs, compact freight evidence, source, accounting inputs, and every VIN before live entry. Capture the schedule-check UTC timestamp with one ordinary terminal clock command after the canonical request write, retain that command's output in the current transcript, and do the comparison directly; a timestamp mentioned by `run.py start`, `run.py ask`, search output, or any existing file is never a valid schedule clock. A complete dashboard freight object is already worker-verified: use its supplied amount and evidence without reopening the dispatch workbook. Re-check dispatch only when evidence is missing, malformed, or contradictory. Never estimate missing freight. Record an absolute 20-minute preflight deadline from that same fresh clock value. Check the deadline once immediately before the first live sheet mutation and once after the verified sheet transaction before opening AutoSoft; do not spend tool calls repeatedly querying the clock between browser actions. If the deadline arrives before mutation, fail closed. Once a sheet block write begins, finish every authorized block and the independent export/readback as one transaction; never stop with a partially written batch.
3. Deduplicate every supplied VIN against the dashboard ledger, only the named store's stocking sheet, and the run checkpoints before writing. Never download or inspect the other store's sheet during this store-isolated run; the dashboard ledger is the cross-store VIN gate. Obey each `batch_manifest_preflight.py` candidate's `sheet_action`: `APPEND_NEW` writes only its reserved blank tail row; `REUSE_EXISTING` never appends or rewrites the row and requires every populated cell to be verified against the current manifest before AutoSoft. `RESUME_EXISTING_TAIL_ROWS`, `REUSE_EXISTING_ROWS`, and `REUSE_EXISTING_AND_APPEND` are valid only when the helper returns `ready: true`; any other existing-row mode is a blocker. A terminal AutoSoft/readback checkpoint is never reposted. Build tab/newline-delimited clipboard blocks from the ordered manifest and paste all new or missing values at once for each contiguous set of authorized columns from the store map; split around unauthorized, trade, or formula columns and never step through cells with Tab/Enter. Then perform a second row-by-row verification pass from one fresh export.
4. Before AutoSoft, run `python tools/focus_autosoft_rdp.py --expected-title <payload store.rdp_window_title>` (quote the actual value in the shell when it contains spaces), capture the exact main RDP window, and visibly verify the inner AutoSoft screen shows payload `store.autosoft_instance`. These are intentionally separate checks. Hold this foreground lease across the batch; do not re-run it at every vehicle. Re-run only after an actual focus/window change or unexplained state. Never send background input to mstsc.exe and never type after an `unverifiable` action until one fresh capture proves state.
5. Finish preflight by building one compact local ordered posting manifest containing every verified vehicle-specific field. Before opening NextGear, locate the newest `Exportable Inventory*.xlsx` and run `python tools/batch_source_preflight.py --nextgear-export <path> --stock-sheet <target-store-export> --vin <VIN1> --vin <VIN2> ... --output <batch-evidence.json>`. Reuse that export when the helper returns `ready: true`: it is at most six hours old and contains one exact `In Stock` row for every payload VIN. This explicitly covers a continuation window from the same load. If it is not reusable, navigate directly to NextGear Tools > Exportable Inventory and make one fresh export; never generate Approved Floorplans as a substitute for ACV/source evidence. Run the helper again and fail closed if it is still not ready. Then run `python tools/batch_manifest_preflight.py --request <canonical-current-payload.json> --stock-sheet <target-store-export> --stock-prefix <store.stock_prefix> --expected-request-id <current webhook request_id> --output <manifest-validation.json>` and require `ready: true`; never author or debug a validator during a live run. Run `python tools/batch_vpic_decode.py --vin <VIN1> --vin <VIN2> ... --output <batch-decode.json>` once and reuse that verified artifact for every AutoSoft record; never author a decoder during a live run. Obey the sheet helper's `plan_mode` and exact candidate rows/stocks. If the reusable export lacks color, use the authenticated NextGear Floor Plans list only as a color supplement: use the configured existing-profile browser attachment once when available, otherwise use the saved-login native flow. On the Cox saved-login flow verify the saved username is already populated, click Next once, require a masked/autofilled password, and click Sign in once. Never type, reveal, or persist the password. In Floor Plans, search each full VIN sequentially in the same semantic search field, require exactly one result, and record the color shown in Description; replace the search with the next VIN and do not open details. Then run `python tools/batch_posting_manifest.py --request <canonical-current-payload.json> --evidence <batch-evidence.json> --validation <manifest-validation.json> --decode <batch-decode.json> --store-registry <current-store-yaml> --output <posting-manifest.json> --color <VIN=COLOR> ...` and require `ready: true`; it creates the ordered posting manifest and exact TSV sheet blocks. Never use `python -c`, PowerShell encoded commands, shell redirection, or hand-authored JSON/TSV to produce these artifacts because headless approval will time out. After the helpers pass, do not enumerate downloads, reread old run artifacts, load unrelated skills, or make broad RAG searches; permit at most one targeted RAG query for a genuinely missing store rule. Use one target-store sheet export to scan all VINs, one transactional write pass for new or resumable rows, and one independent export/readback. Do not revisit the sheet, dispatch workbook, NextGear, or source browser between AutoSoft records unless a specific verification fails. Keep one foreground LA/Columbia session, reuse the already-open Accounting/Inventory workflow, and move directly to the next fresh record after each verified save.
6. Process vehicles strictly in payload order. Send PROCESSING for a child immediately before touching its AutoSoft record. For each text field use verified clipboard entry: focus the intended control, Ctrl+A, copy/paste the exact manifest value, then visually/readback verify it. Start a fresh record, clear and visibly verify the entire VIN field is blank, paste only that VIN, and decode before touching Line. In the legacy purchase entry, currency fields use implied cents (`500.00` is typed as `50000`). Enter the debit template and freight first, set Invoice Amount to ACV, then enter the credit template; never type the dynamic inventory debit row amount. Freight GL 31105 belongs on its populated freight credit row, never the reserved Cash Purchase row, and every nonzero amount must have the intended GL before Post. If `9T` and GL 24100 are already visible, do not re-edit Line; otherwise set its numeric and suffix controls separately and re-verify `9T`, Used, and GL 24100. Apply vehicle-specific mileage, color, cylinders, and source from the manifest, verify accounting, save, and read back. Before the next child can become PROCESSING or its stock can be entered, use `batch_checkpoint.py record`, commit/push the exact manifest and checkpoint non-interactively, use `batch_checkpoint.py callback`, send that payload with `dashboard_callback.py`, and require an accepted/idempotent COMPLETED response. Never hand-author JSON, leave two children PROCESSING, or paste a multi-vehicle block into AutoSoft.
7. Known LA startup recovery: `CRITICAL ERROR ID 227, Position Error #7-0 in AAJ3` shown on the LA City home screen is a one-dismiss startup alert, not automatically a store-wide outage. Click OK once, require the normal `LA City Cars` home screen, open Accounting, and continue only if the password/PIN prompt appears and accepts the configured saved credential. If the alert reappears inside the posting workflow, Accounting cannot open, authentication is rejected, or controls remain unusable, then fail the batch as `AUTOSOFT_UNAVAILABLE`. Never dismiss any other critical/runtime error by analogy.
8. If Accounting reports an `Incomplete Direct Posting Entry`, dismiss the alert once and inspect the recovery list without choosing `Finish Posting`. Record its operator/date/stock/VIN suffix. Continue only after the incomplete entry has been independently resolved and a clean re-login no longer shows it. Never finish, delete, or overwrite an unrelated or collision-rejected draft; the correction path requires separately authorized AutoSoft correction access or AutoSoft support. Return the exact blocker with `failure_scope=BATCH`.
9. A vehicle-specific failure gets its own FAILED callback and the batch continues. A wrong store, lost/unusable AutoSoft session, rejected accounting authentication, persistent AAJ3/runtime error, unresolved incomplete posting, or other store-wide blocker requires ONE current-child FAILED callback with `failure_scope=BATCH`; do not spend repeated turns trying blind recovery. The dashboard deterministically fails every still-claimed sibling and releases the shared lock.
   If a rejected Post leaves an exact stock/VIN shell with zero original inventory, zero internals, and no active journal data, do not retry that stock, delete it, or call it posted. Record a vehicle-scoped failure requiring authorized AutoSoft Edits & Corrections recovery, then continue only after the store session is demonstrably clean.
10. Update the RAG record after every vehicle checkpoint and finish with a concise batch summary. Never repost a vehicle whose callback/checkpoint is already terminal. Treat repeated identical tool failures as a blocker after the second occurrence. Target at most 20 minutes for shared preflight and 15 minutes per AutoSoft record. A preflight timeout may stop before the first sheet mutation or after a complete verified sheet transaction, never between its clipboard blocks. If a safety gate cannot be established within that budget, fail closed with the exact phase instead of looping.

Efficiency rule: a screenshot is evidence for a screen transition or checkpoint, not a polling mechanism. In Chrome, prefer semantic element indexes and direct Ctrl+L navigation over raw tab coordinates. After an unverifiable browser action, take one targeted SOM/AX capture and either advance or stop after the second identical failure. Avoid repeated full-screen captures between deterministic actions on an unchanged verified screen. Prefer compact terminal/readback results and combine safe consecutive clipboard actions when the computer-control tool supports a sequence.

The callback's stock number, ACV, freight, final total, source/run summary, and RAG commit must come from verified evidence. A missing source, missing defensible freight, duplicate/store conflict, rejected PIN, unexpected screen, or accounting mismatch must fail closed; never estimate or bypass a gate.
